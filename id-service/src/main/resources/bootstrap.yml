spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://www.zyinux.com:8848
        namespace: sqs
      config:
        enabled: true
        server-addr: http://www.zyinux.com:8848
        file-extension: yml
        namespace: sqs
  application:
    name: id-service
  profiles:
    active: dev

